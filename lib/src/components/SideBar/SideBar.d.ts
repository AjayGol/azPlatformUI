import React from 'react';
import { SideBarProps } from './SideBar.types';
export declare const SideBar: ({ list, onPressItem, selectedItem, disableFooter, disableHeader, header, footer, slideIn, }: SideBarProps) => React.JSX.Element;
