import React from 'react';
import { CustomChipProps } from './CustomChip.types';
declare const CustomChip: ({ label, icon, iconLib, iconSize, onPressIcon, value, iconColor, }: CustomChipProps) => React.JSX.Element;
export default CustomChip;
